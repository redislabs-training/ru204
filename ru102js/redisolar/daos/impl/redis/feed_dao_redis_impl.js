const insert = meterReading => 'Redis TODO';

const getRecentGlobal = limit => 'Redis TODO';

const getRecentForSite = (siteId, limit) => 'Redis TODO';

module.exports = {
  insert,
  getRecentGlobal,
  getRecentForSite,
};
