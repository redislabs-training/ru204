const insert = meterReading => 'Static TODO';

const getRecentGlobal = limit => 'Static TODO';

const getRecentForSite = (siteId, limit) => 'Static TODO';

module.exports = {
  insert,
  getRecentGlobal,
  getRecentForSite,
};
